import 'package:flutter/material.dart';

class Vended extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(title: Text('Pagina Ventas'),),
      body: new Column(
        children: <Widget>[
          new Text("vended"),
        ],
      ),
    );
  }
}